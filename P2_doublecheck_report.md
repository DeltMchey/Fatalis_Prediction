# P2 双重检查报告

> **日期**: 2026-06-25  
> **目的**: 对 `P2_execution_plan.md` 中识别的 3 个剩余任务（P2.4、P2.5、P2.6）进行严格审查  
> **参考来源**: `memory-bank/progress.md`（路线图）  
> **状态**: 未修改任何代码。仅分析。

---

## 审查文件

| 文件 | 行数 | 审查目的 |
|------|------|-----------|
| `memory-bank/progress.md` | 84 行 | 路线图的 P2/P3/P4/P5 边界定义 |
| `memory-bank/activeContext.md` | 91 行 | 硬约束及阶段排除边界 |
| `src/config/actions.py` | 125 行 | 检查现有提取内容（P2.4 的目标文件） |
| `ai_engine.py` | 299 行 | 姿态 FSM 重复项（P2.4），`except: pass` 残留（P2.1） |
| `data_cleaner.py` | 96 行 | 姿态 FSM 重复项（P2.4） |
| `train_lgbm.py` | 82 行 | 裸 except（P2.5） |
| `enrage.py` | 89 行 | 3 个裸 except（P2.6） |

---

## 1. 路线图的 P2 定义（权威来源）

根据 `memory-bank/progress.md`，P2 = "P0 修复（关键修复）"：

| 路线图项目 | 描述 | 状态 |
|-------------|------|--------|
| Tech_debt #3 | 用结构化日志替换**所有** `except: pass` | ✅ 已提交 (`ea143d5`) |
| Tech_debt #2 | 将 ACTION_MAPPING 统一为单一真实来源 | ✅ 已提交 (`43bfcff`) |
| Tech_debt #1 | 将内存偏移量集中到配置中 | ✅ 已提交 (`cb4d218`) |

**关键观察**: P2.1 的提交 diff 显示只有 `ai_engine.py` 被修改。任务描述说的是"所有 `except: pass`"——暗示项目范围内的修复。`train_lgbm.py`、`enrage.py` 或 `data_cleaner.py` 均未在 P2.1 中被触及。

### 阶段边界

| 阶段 | 范围 | 关键词 |
|-------|-------|----------|
| P2 | 关键修复（重复项消除、日志记录、配置集中化） | "高优先级技术债务" |
| P3 | 测试套件 + CI | "测试体系" |
| P4 | God Class 拆分 → 模块 | "架构重构" |
| P5 | 模型版本管理、增量训练、发布 | "模型工程化" |

---

## 2. 逐任务双重检查

---

### P2.4: 将姿态 FSM 转移集统一到 `src/config/actions.py`

#### 审查内容

**ai_engine.py:222-228**（实时代码）:
```python
if action in {115, 116, 121, 122, 129, 119, 98, 99, 68, 69, 70, 71, 72, 149, 150, 151, 32, 19, 197, 219, 222}:
    shared_state['posture'] = 1
elif action in {138, 49, 50, 51, 52, 73, 30, 74, 75}:
    shared_state['posture'] = 0
elif action in {107, 108, 167, 179}:
    shared_state['posture'] = 2
```

**data_cleaner.py:49-55**（数据管道）:
```python
if action_id in {115, 116, 121, 122, 129, 119, 98, 99, 68, 69, 70, 71, 72, 149, 150, 151, 32, 19, 197, 219, 222}:
    current_posture = 1
elif action_id in {138, 49, 50, 51, 52, 73, 30, 74, 75}:
    current_posture = 0
elif action_id in {107, 108, 167, 179}:
    current_posture = 2
```

#### 集合比较

| 集合 | ai_engine.py | data_cleaner.py | 匹配？ |
|------|-------------|-----------------|--------|
| → 姿态 1（站立） | `{115,116,121,122,129,119,98,99,68,69,70,71,72,149,150,151,32,19,197,219,222}` (21 个 ID) | 相同 (21) | ✅ 逐位相同 |
| → 姿态 0（趴下） | `{138,49,50,51,52,73,30,74,75}` (9 个 ID) | 相同 (9) | ✅ 逐位相同 |
| → 姿态 2（飞行） | `{107,108,167,179}` (4 个 ID) | 相同 (4) | ✅ 逐位相同 |

#### 判定

| 问题 | 答案 | 理由 |
|--------|------|----------|
| **1. 这真的是 P2 任务吗？** | ✅ **是** | 与 P2.2（ACTION_MAPPING 重复）的结构模式完全相同：在两个文件中由集合字面量重复组成的共享业务逻辑常量。任何单方面的未来变更都可能导致静默数据损坏。 |
| **2. 它属于 P3/P4 吗？** | ❌ **否** | 不是 P3（无测试组件）不是 P4 — P4 提取完整的类/模块（StateTracker、MemoryReader 等）；这里是常量提取到已有的配置文件中，遵循的是 P2.2 已经建立的完全相同的模式。 |
| **3. 它引入了什么风险？** | 🟢 **几乎为零** | 集合值完全不变地移动。两者逐位验证为相同。在 3 个地方（1 个定义 + 2 个消费者）中，21+9+4 = 34 个唯一动作 ID 保持不变。唯一的"风险"是导入路径错别字——会在导入时失败，而不是静默损坏。 |
| **4. 能在不改变行为的情况下完成吗？** | ✅ **是** | 将内联集合字面量替换为命名常量，值相同。`data_cleaner.py` 的输出在 `diff` 下保持不变。运行时行为保持不变。 |

#### 附加说明

`ai_engine.py:256-262` 中的**预测过滤器**集合是不同的集合（`PRONE_FILTER` / `STAND_FILTER` 用于 AI 概率归零）。这些集合仅在 `ai_engine.py`（无重复）中出现，但是为了完整性，不妨将它们提取到 `src/config/actions.py` 中。此额外提取：
- **属于 P2 范围** — 与 P2.4 模式相同；非破坏性常量提取
- **本次冲刺可选** — 与 P2.4 核心重复修复的优先级不同
- 如果包含，总工作量约 15 分钟

#### 建议

**作为 P2 任务继续。** 这是最干净的 P2 候选项——消除重复、无风险、工作量 30 分钟。与已完成的 P2.2 结构完全相同。

---

### P2.5: 修复 `train_lgbm.py:15` 裸 `except:`

#### 审查内容

```python
# train_lgbm.py:14-15
    try: df = pd.read_csv("data/ML_Ready_Dataset.csv")
    except: return print("❌ 找不到 data/ML_Ready_Dataset.csv！")
```

#### 判定

| 问题 | 答案 | 理由 |
|--------|------|----------|
| **1. 这真的是 P2 任务吗？** | ✅ **是** | 裸 `except:` 会捕获 `KeyboardInterrupt` 和 `SystemExit`。P2.1 的任务描述指定的是"**所有** `except: pass`"——这个是遗漏的。它使用 `print()` 而不是 `logger.error()`，完全绕过了 P2.1 创建的结构化日志基础设施。 |
| **2. 它属于 P3/P4 吗？** | ❌ **否** | 不是 P3（无测试工件）。不是 P4 — 不是架构变更；只是将 `except:` 改为 `except Exception:` 并添加日志导入。 |
| **3. 它引入了什么风险？** | 🟢 **几乎为零** | 当前行为：Ctrl+C 打印一条错误并退出。期望行为：Ctrl+C 仍然退出；缺少 CSV 会记录到 `blackdragon.log` 并打印。输出在语义上相同。 |
| **4. 能在不改变行为的情况下完成吗？** | ✅ **是** | 将裸 `except:` 改为 `except Exception:` —— KeyboardInterrupt/SystemExit 继续向上传播。训练管道、模型保存、特征重要性图：均未触及。 |

#### 建议

**作为 P2 任务继续。** 这是 P2.1 最明显的遗漏。工作量 5 分钟。零风险。

---

### P2.6: 修复 `enrage.py` 裸 `except:` 模式

#### 审查内容

`enrage.py` 中的三个位置：

| 行号 | 代码 | 上下文 |
|------|------|---------|
| 13 | `except: return 0` | `get_ptr()` — 指针链遍历失败 |
| 30 | `except: pass` | `find_monster()` — 槽位迭代，逐个槽位尝试 |
| 72 | `except: print(...)` | 内存扫描循环 — 浮点数读取失败 |

#### 判定

| 问题 | 答案 | 理由 |
|--------|------|----------|
| **1. 这真的是 P2 任务吗？** | ⚠️ **边界情况** | `enrage.py` 是一个独立的诊断/逆向工程工具。它不参与数据管道（`data_cleaner.py → train_lgbm.py`）或推理（`ai_engine.py`）。该文件有 `__main__` 守卫，永远不被导入。然而：P2.1 说的是"所有 `except: pass`"，P2.3 已经为了 `OFFSETS` 导入修改了 `enrage.py`，这意味着它在 P2 范围内。严格来说，项目范围内的裸 except 修复属于 P2。但实际上，诊断工具并不是"关键修复"。 |
| **2. 它属于 P3/P4 吗？** | 🤷 **可论证** | 可以合理地归类为 P4 清理（"清理诊断代码"）。它绝对不属于 P3。一个公正的解读会将其归类为低优先级的 P2 或杂项 P4 范围的项目。 |
| **3. 它引入了什么风险？** | 🟢 **零** | 独立的诊断工具。对数据管道、模型训练或实时推理没有影响。第 30 行（`except: pass`）在语义上正确——在怪物槽位链上迭代，预期大多数槽位会失败。 |
| **4. 能在不改变行为的情况下完成吗？** | ✅ **是** | 将裸 `except:` 改为 `except Exception:` + 相同回退逻辑。输出完全相同。 |

#### 建议

**低优先级继续，或降级为 P4 "顺带"项**。10 分钟的工作量在 P2 中并非是糟糕的时间花费，但这个文件从技术上来说并没有损坏。如果 P4 即将开始，将其推迟到那时作为"顺带清理"工作是合理的。

---

## 3. 最终建议

### 按推荐处置顺序汇总

| 顺序 | 任务 | 判定 | 处置 | 工作量 |
|------|------|--------|------------|--------|
| 1 | **P2.4** 统一姿态 FSM 转移集 | ✅ 确认为 P2 | **执行** — 与 P2.2 模式完全相同 | 30m |
| 2 | **P2.5** 修复 `train_lgbm.py` 裸 except | ✅ 确认为 P2 | **执行** — P2.1 最清晰的遗漏项 | 5m |
| 3 | P2.4 扩展：提取预测过滤器集合 | ✅ 属于 P2 | **可选** — 完整性改进 | 15m |
| 4 | **P2.6** 修复 `enrage.py` 裸 except | ⚠️ 边界/低优先级 P2 | **执行或推迟** — 根据冲刺速度判断 | 10m |

### 如果 P2 冲刺窗口紧张

```text
最小值:  P2.5 (5m) + P2.4 (30m) = 35 分钟
标准值:  P2.5 (5m) + P2.4 (30m) + P2.6 (10m) = 45 分钟
最大:    以上 + 预测过滤器提取 (15m) = 60 分钟
```

### 硬约束验证（所有 3 项任务）

| 约束条件 | P2.4 | P2.5 | P2.6 |
|------------|------|------|------|
| 无架构重构 | ✅ | ✅ | ✅ |
| 无模块分解 | ✅ | ✅ | ✅ |
| 无行为变更 | ✅ 集合值不变 | ✅ 仅更改错误处理路径 | ✅ 仅更改错误处理路径 |
| 无模型变更 | ✅ | ✅ | ✅ |
| 无玩法变更 | ✅ | ✅ | ✅ |

### 扩展范围：值得注意但不立即执行的内容

双重检查发现了一个额外的模式，不属于当前的任何 P2.* 任务，但值得注意：

1. **`ai_engine.py:256-262` 中的预测过滤器集合** — 不是在不同文件之间重复，而是在 `ai_engine.py.Update_logic()` 方法中作为内联字面量。将它们提取到 `src/config/actions.py` 中的命名常量，将使所有"物理过滤器"规则在同一个配置文件中，从而提高可维护性。这自然属于 P2.4，但并非严格必需——它是一个重复（字面量格式），而不是跨文件的副本。

2. **`ai_engine.py:61` 的 `except Exception: pass`** — 在怪物槽位扫描循环内。从功能上看是正确的（预期大多数槽位会失败），但静默地丢弃异常。P2.1 将其替换为适当类型的异常处理程序加上 `pass`。这个仍然有 `pass`，但它现在是一个 `except Exception` 而不是裸 `except`，并且正在使用 `pass`（故意静默）。可论证为"按设计"——不需要更改。

---

*双重检查完成。未修改任何代码。未生成补丁。*
