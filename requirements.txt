# BlackDragon — AI-assisted hunting overlay for Monster Hunter World
# Install: pip install -r requirements.txt
#
# Runtime dependencies (Windows-only project; pymem/dearpygui require Win32 API).
# Test dependencies (pytest, pytest-cov) are installed separately by CI — see
# .github/workflows/test.yml.

dearpygui==2.3
joblib==1.5.3
lightgbm==4.6.0
matplotlib==3.10.9
numpy==2.4.4
pandas==3.0.2
Pymem==1.14.0
scikit-learn==1.8.0
# P7 采纳（AutoML Run B）：生产模型为 xgboost pipeline（libxgboost.dll 随包分发）；
# scipy 为 xgboost 的传递依赖。依据规划 5.4/P7：xgboost 胜者采纳后进入运行时依赖。
scipy==1.18.0
xgboost==3.4.1
